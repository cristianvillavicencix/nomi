# Clients cleanup — applied (2026-06-02)

**Database:** `qjglkywmqwqdoaboakao`

## Backup

- `supabase db dump` failed (Docker not running).
- Pre-apply JSON exports: `scripts/cleanup/backups/companies_pre_cleanup.json` (~146 KB), `contacts_pre_cleanup.json` (~352 KB).

## 01 — primary_contact_id

- **Applied:** 146 companies backfilled.
- **Result:** 156 with explicit FK; 0 null-with-contacts remaining; 37 companies still have no contacts.

## 02 — phone in last_name

- **Applied:** 3 contacts (1713, 1716, 1728).
- `last_name` cleared; numbers moved to `phone_jsonb`.

## 03 — sector normalization

Manual map (all → `other`):

| id | name | was | now |
|----|------|-----|-----|
| 588 | Joseph Handyman Construction LLC | utilities | other |
| 686 | Pinos Multiservice LLC | utilities | other |
| 772 | SmartFix Home Improvement LLC Roofing Experts | Management ISV | other |
| 778 | ThunderPro Construction LLC | information-technology | other |

All label sectors (Roofing, Restoration, etc.) → trade slugs. **0** non-slug sectors remain.

## 04 — contact status

- `contact` → `contact_only` (224)
- `warm` / `cold` → `prospect` (then some promoted to `client` via primary-contact trigger)
- `new` → `lead` (1)

**Final distribution:**

| status | count |
|--------|------:|
| contact_only | 224 |
| lead | 68 |
| client | 18 |

Legacy statuses (`contact`, warm, cold, new): **0**

DB triggers `promote_contact_to_client` and `promote_contact_to_client_on_contact_change` updated to `(lead, prospect)` buckets.

## Side effect (01 + triggers)

Backfill set `primary_contact_id` on 146 companies → trigger promoted **6** additional contacts from lead buckets to `client` (12 → 18).
