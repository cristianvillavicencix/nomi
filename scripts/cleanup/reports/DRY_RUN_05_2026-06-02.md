# Dry-run: 05_extend_primary_contact_triggers

**Date:** 2026-06-02  
**Target:** hosted Supabase `qjglkywmqwqdoaboakao`  
**Script:** `scripts/cleanup/05_extend_primary_contact_triggers.sql`  
**Status:** DRY-RUN ONLY — not applied

## D.1 — Promote primary contacts to `client`

| Metric | Count |
|--------|------:|
| `contact_only` contacts who are `companies.primary_contact_id` | **138** |
| All non-`client` contacts who are company primary | **138** |

On apply, these 138 rows would be updated to `status = 'client'` via backfill, and future assignments would promote from any non-client status via `promote_contact_to_client()`.

## D.2 — Demote outgoing primary

Demotion is **event-driven** (no bulk backfill): when `companies.primary_contact_id` changes, the outgoing contact becomes `contact_only` if they are not primary on any other company.

| Metric | Count |
|--------|------:|
| `client` contacts neither primary nor linked to a deal (info only; not auto-demoted) | **0** |

## Apply instructions

1. Review counts above.
2. Open `scripts/cleanup/05_extend_primary_contact_triggers.sql` and set `apply_changes := true`.
3. Run: `npx supabase db query --linked --agent=no -f scripts/cleanup/05_extend_primary_contact_triggers.sql`

**Awaiting explicit OK before apply.**
