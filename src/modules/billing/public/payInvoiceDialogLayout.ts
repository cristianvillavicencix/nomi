/** Shared responsive shell for public invoice payment (dialog + full-page pay mode). */
export const publicInvoicePaymentShellClass =
  "mx-auto w-full max-w-[560px] overflow-hidden bg-background shadow-none max-sm:rounded-none max-sm:border-0 sm:rounded-2xl sm:border sm:border-border/50 sm:shadow-sm";

/** Payment link (?pay=1): narrow on mobile, two-column card on desktop. */
export const focusPaymentEntryShellClass =
  "mx-auto w-full max-w-[440px] overflow-hidden rounded-2xl border border-border/50 bg-background shadow-sm lg:max-w-4xl";

export const publicInvoicePaymentSectionPadding = "px-4 sm:px-6";
